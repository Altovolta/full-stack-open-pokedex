name: Hello World!

on:
  push:
    branch:
      - main

jobs:
  hello_world_job:
    runs-on: ubuntu-latest
    steps:
      - name: Say hello 
        run: | 
          echo "Hello World!"
      - name: Currenent date
        run: |
          date 
      - name: Directory content
        run: |
          ls -l
